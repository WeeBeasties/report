# Results {#results -}

Now I'll teach you some crazy math, but I need to work it out first... Let's see if we can get some plotting going.

```{r myplot, echo=FALSE, fig.cap="Sepal length as a function of sepal width", fig.width=6, fig.asp=0.6}

plot(iris$Sepal.Length~iris$Sepal.Width)

```
